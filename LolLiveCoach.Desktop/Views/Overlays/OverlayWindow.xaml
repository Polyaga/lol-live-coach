<Window x:Class="LolLiveCoach.Desktop.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="430"
        Height="240"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="{StaticResource TextBrush}"
        ResizeMode="NoResize"
        ShowActivated="False"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStyle="None">
    <Border Padding="18"
            CornerRadius="22"
            Background="#D9101725"
            BorderBrush="#FF4F9CF9"
            BorderThickness="1.2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock FontSize="13"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource AccentBrush}"
                           Text="LIVE COACH" />

                <Border Grid.Column="1"
                        Padding="10,4"
                        CornerRadius="999"
                        Background="#1C2436"
                        BorderBrush="#2B3A55"
                        BorderThickness="1">
                    <TextBlock x:Name="OverlayPhaseText"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Text="Laning" />
                </Border>
            </Grid>

            <TextBlock x:Name="OverlayMainAdviceText"
                       Grid.Row="1"
                       Margin="0,14,0,0"
                       FontSize="22"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"
                       Text="Le conseil principal apparait ici." />

            <TextBlock x:Name="OverlaySecondaryAdviceText"
                       Grid.Row="2"
                       Margin="0,12,0,0"
                       Foreground="{StaticResource MutedBrush}"
                       FontSize="14"
                       TextWrapping="Wrap"
                       Text="Details contextuels et rappel de tempo." />

            <DockPanel Grid.Row="3" Margin="0,16,0,0">
                <Border DockPanel.Dock="Top"
                        Margin="0,0,0,10"
                        Padding="12"
                        CornerRadius="14"
                        Background="#182235"
                        BorderBrush="#27344A"
                        BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="OverlayRoleText"
                                   FontWeight="SemiBold"
                                   Text="ADC" />
                        <TextBlock x:Name="OverlayKdaText"
                                   Grid.Column="1"
                                   Foreground="{StaticResource MutedBrush}"
                                   Text="KDA 0/0/0" />
                    </Grid>
                </Border>

                <ItemsControl x:Name="OverlayAlertsItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,8"
                                    Padding="10"
                                    CornerRadius="12"
                                    Background="#1A2436"
                                    BorderBrush="#26364E"
                                    BorderThickness="1">
                                <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>
        </Grid>
    </Border>
</Window>
